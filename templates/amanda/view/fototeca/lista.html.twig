{% import "fields.html.twig" as fields %}
{% import "table.html.twig" as tables %}
{% import "buttons.html.twig" as buttons %}

<div class="centercontent">

    <div class="pageheader notab">
        <h1 class="pagetitle">{{titlePage}}</h1>
        {{ permissoes['FOTOT_INSERIR'] ? buttons.iconButton("Inserir novo", "fototeca/form", "no_icon", "green") : ""}}
        <br clear="all">
    </div><!--pageheader-->

    <div id="contentwrapper" class="contentwrapper">
        <div class="subcontent">
            {{ tables.table(
                            [
                            tables.head([
                                            tables.columnHeader(fields.checkboxHeader()),
                                            tables.columnHeader([
                                                                permissoes['FOTOT_PUBLI'] ? buttons.iconButton("Publicar", "javascript:alterarStatus(1)", "no_icon publicar") : "",
                                                                permissoes['FOTOT_NOPUBLI'] ? buttons.iconButton("Não publicar", "javascript:alterarStatus(0)", "no_icon despublicar") : "",
                                                                permissoes['FOTOT_ALTPOS'] ? buttons.iconButton("Salvar posicionamento", "javascript:salvarPosicionamento()", "no_icon") : "",
                                                                permissoes['FOTOT_EXCLUIR'] ? buttons.iconButton("Excluir", "javascript:excluir()", "no_icon deletebutton") : "",
                                                                "&nbsp;&nbsp;&nbsp;&nbsp; Data ", 
                                                                fields.date("data_inicial", "", "", ""),
                                                                "&nbsp;&nbsp;", 
                                                                fields.date("data_final", "", "", ""),
                                                            ]|join, "80%"),
                                            tables.columnHeader(fields.select("status", status, "", "Todos os status"))
                                        ])
                            ]|join,
                    "fototecas") }}

        </div>
    </div><!--contentwrapper-->

    <br clear="all" />

</div>