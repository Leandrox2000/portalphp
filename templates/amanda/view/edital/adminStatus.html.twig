{% import "fields.html.twig" as fields %}
{% import "table.html.twig" as tables %}
{% import "buttons.html.twig" as buttons %}

<script type="text/javascript">
    
var ordemColumns = {{ columns | json_encode | raw }};

var dumpRows = {};
    
</script>
<div class="centercontent">

    <div class="pageheader notab">
        <h1 class="pagetitle">{{ titlePage|default('Status') }}</h1>
    </div><!--pageheader-->

    <div id="contentwrapper" class="contentwrapper">

        <div class="subcontent">
            {% if permissoes['CADAS_SALVAR'] %}
                {{ fields.text("novoStatus") }}
                {{ buttons.iconButton("Adicionar", "javascript:adicionarStatus()", "no_icon") }}
                {{ buttons.iconButton("Salvar posicionamento", "javascript:salvarPosicionamento()", "no_icon") }}
            {% endif %}
            <br clear="all" />
            <br clear="all" />
            
            {{ status|raw }}
            
        </div>

    </div><!--contentwrapper-->

    <br clear="all" />

</div>
